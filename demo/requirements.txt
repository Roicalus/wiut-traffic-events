# Живое демо (demo/app.py) — локально или на Hugging Face Spaces (ZeroGPU). Сабмиту не
# нужно: организаторы ставят только ../requirements.txt. На ZeroGPU пакет spaces и gradio
# уже установлены; torch — обычная CUDA-сборка (на CPU она тоже работает).
torch==2.8.0
torchvision==0.23.0
ultralytics==8.4.160
opencv-python>=4.8,<5,!=4.13.0.90
numpy>=1.24,<3
lap>=0.5.12
gradio==5.50.0
plotly==7.1.0
imageio-ffmpeg==0.6.0
